#!/bin/bash

if [[ $DISABLE_HTTPS -ne 1 ]] && \
   [[ $ENABLE_LETSENCRYPT -eq 1 ]]; then
    exec cron -f
else
    # if cron should not be started,
    # prevent supervisor from restarting this script again and again
    while true; do
        sleep 60;
    done
fi
